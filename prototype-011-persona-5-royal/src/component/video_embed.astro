---
interface Props {
  url: string;
  title: string;
}
const { url, title } = Astro.props;
---
<div class="video-embed">
  <iframe src={url} title={title} allowfullscreen loading="lazy"></iframe>
</div>
<style>
  .video-embed { position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; clip-path: polygon(0 0, calc(100% - 16px) 0, 100% 16px, 100% 100%, 16px 100%, 0 calc(100% - 16px)); }
  .video-embed iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; border: 0; }
</style>
